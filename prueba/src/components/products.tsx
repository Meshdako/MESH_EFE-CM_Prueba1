import React from "react";
import { getFakestore } from "../services/fakestore.service";
import { FakeStore, Data, Rating } from "../interfaces/fakestore.interface"



function Products({data}: FakeStore) {

    return ( 
        <div>
            <h1>Products</h1>
            
        </div>
    );
}

export default Products;